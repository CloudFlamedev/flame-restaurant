// This file can be regenerated at container startup (see docker-entrypoint.sh)
// to point the frontend at the correct backend URL per environment.
window.FLAME_API_BASE = "http://localhost:8000/api";
